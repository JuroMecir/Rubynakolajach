<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <p> <%= gravatar_for @user %> </p>
      <span>  <%= "Meno:" %> <%= @user.name %> </span>
      <span><%= "Priezvisko:" %> <%= @user.surname %></span>
      <span> <%= "Vek:" %> <%= @user.age %> </span>
      <span> <%= "E-mail:" %> <%= @user.email %> </span>
    </section>
  </aside>
  <div class="col-md-8">
    <% if @user.catches.any? %>
      <h3>Catches:</h3>
      <ol class="catches">
        <%= render @catches %>
      </ol>

    <% end %>
  </div>
</div>

